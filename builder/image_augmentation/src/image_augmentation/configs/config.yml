general:
  logging:
    console:
      _type: console
      level: INFO

  front_end:
    _type: fastapi

workflow:
  _type: image_augmentation
  api_endpoint: "https://ai.api.nvidia.com/v1"
  redis_url: "redis://daedalus-redis.daedalus.svc.cluster.local:6379"
  api_key: null  # Set if your API requires authentication or use OPENAI_API_KEY env var
  timeout: 300.0
  model: "gpt-image-1"
  image_config: null  # Optional: { "aspect_ratio": "16:9", "image_size": "1K" }
