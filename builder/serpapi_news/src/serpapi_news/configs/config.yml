general:
  logging:
    console:
      _type: console
      level: INFO

  front_end:
    _type: console

workflow:
  _type: serpapi_news
  # api_key: "your-serpapi-key-here"  # Optional: Can also use SERPAPI_KEY env var
