{{- if and .Values.backend.enabled .Values.backend.env.createSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "daedalus.fullname" . }}-backend-env
  labels:
    {{- include "daedalus.labels" . | nindent 4 }}
type: Opaque
data:
{{- range $k, $v := .Values.backend.env.data }}
  {{ $k }}: {{ $v | quote }}
{{- end }}
{{- end }}
