{{- if .Values.backend.default.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "daedalus.fullname" . }}-backend-default
  labels:
    {{- include "daedalus.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend-default
spec:
  replicas: 1
  revisionHistoryLimit: 3
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "daedalus.name" . }}
      app.kubernetes.io/component: backend-default
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "daedalus.name" . }}
        app.kubernetes.io/component: backend-default
        app.kubernetes.io/instance: {{ .Release.Name }}
      annotations:
        deploy-timestamp: {{ .Values.forceRedeploy | default "" | quote }}
    spec:
      {{- include "daedalus.nodePlacement" . | nindent 6 }}
      {{- with .Values.backend.default.securityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: backend
          image: "{{ .Values.images.backend.repository }}:{{ .Values.images.backend.tag }}"
          imagePullPolicy: {{ .Values.images.backend.pullPolicy }}
          ports:
            - containerPort: {{ .Values.backend.default.port }}
              name: http
          startupProbe:
            tcpSocket:
              port: http
            failureThreshold: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /docs
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /docs
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
          {{- with .Values.backend.default.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          envFrom:
            - secretRef:
                name: {{ .Values.backend.default.env.fromSecret | default (printf "%s-backend-env" (include "daedalus.fullname" .)) }}
          {{- with .Values.backend.default.env.overrides }}
          env:
            {{- range $k, $v := . }}
            - name: {{ $k }}
              value: {{ $v | quote }}
            {{- end }}
          {{- end }}
          volumeMounts:
            {{- if .Values.backend.default.config.enabled }}
            - name: backend-config
              mountPath: /workspace/config.yaml
              subPath: config.yaml
            {{- end }}
            {{- if .Values.backend.persistence.enabled }}
            - name: backend-data
              mountPath: /data
            {{- end }}
            {{- if .Values.backend.skills.enabled }}
            - name: skills
              mountPath: {{ .Values.backend.skills.mountPath | default "/skills" }}
              readOnly: true
            {{- end }}
      {{- if or .Values.backend.persistence.enabled .Values.backend.skills.enabled .Values.backend.default.config.enabled }}
      volumes:
        {{- if .Values.backend.persistence.enabled }}
        - name: backend-data
          persistentVolumeClaim:
            claimName: {{ include "daedalus.fullname" . }}-backend
        {{- end }}
        {{- if .Values.backend.default.config.enabled }}
        - name: backend-config
          configMap:
            name: {{ include "daedalus.fullname" . }}-backend-default-config
        {{- end }}
        {{- if .Values.backend.skills.enabled }}
        - name: skills
          {{- if .Values.backend.skills.pvcName }}
          persistentVolumeClaim:
            claimName: {{ .Values.backend.skills.pvcName }}
          {{- else }}
          emptyDir: {}
          {{- end }}
        {{- end }}
      {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "daedalus.fullname" . }}-backend-default
  labels:
    {{- include "daedalus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.default.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "daedalus.name" . }}
    app.kubernetes.io/component: backend-default
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - port: {{ .Values.backend.default.service.port }}
      targetPort: http
      name: http
{{- end }}
