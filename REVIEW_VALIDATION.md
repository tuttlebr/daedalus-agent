# Review validation record

Pinned target: `main` / `457d338ad9eec9a285a57436b37213b5ba577477`. Original workspace initially clean. Commands ran in the detached isolated checkout `/tmp/daedalus-review-457d338` and disposable test environments, not the original production workspace. The evidence root is `/tmp/daedalus-review-evidence-457d338`. Exact per-check commands/cwd/runtime/status/logs follow. Path shorthands are explicitly defined in their section. All shown credentials/certificates belong to disposable fixtures.

Makefile and CI were read first. Independent checks were run separately rather than through fail-fast `make ci`; deployment/publishing targets were not invoked. Test assertions and locks were preserved. Temporary reproduction tests assert the observed defect and are not repaired production tests. Failed harness attempts, disproved hypotheses, initial browser failure and formatter failures remain recorded.

## Outcome summary

| Area | Status | Result |
|---|---|---|
| Python suite + coverage | PASS |1327 passed/4 opt-in skips;75.70% configured coverage |
| Python real Redis | PASS |4 passed; native Redis client also exercised for memory-clear reproduction |
| Frontend unit + real Redis | PASS |818 passed/156 opt-in skips;177 passed real Redis;47.60% lines |
| Lint/type/production/worker/WebSocket builds | PASS | Node 22, locked install, separate image build |
| Browser matrix | FAIL, focused retry PASS |150 passed/14 skipped/1 login timeout; same-build failed case retry 1 passed |
| NAT pinned runtime/catalog | PASS | Native NAT 1.8/17 tools/18 skills/102 resources; remote service fixtures |
| Helm/nginx/Redis upgrade | PASS | Default/custom renders, actual nginx-t, disposable Kind persisted ACL/TLS upgrade/rollback |
| Compose exact Redis startup | DEFECT REPRODUCED | Default username exits1; named user health passes locally but remote auth denied |
| Security | MIXED | Production npm/built image gates pass; release scan false-positive mismatch fails; omitted upstream scans find affected dependencies; primary applicability triaged |
| Read-only Python static/style | MIXED | Ruff lint/Bandit pass; pinned Ruff-format/isort fail worker.py only |
| Required external/other-platform checks | BLOCKED / NOT RUN | No live infrastructure allowed; disposable full upstream equivalents unavailable; no arm64/physical device/enforcing CNI/load test |

## Baseline, isolation and evidence preservation

Baseline inventory command: `git ls-files -z` with tracked-file size/type/line inventory, on the original branch/commit; initial `git status --porcelain`, branch, commit and origin recorded in the report and final `root/baseline-metadata.json`. The initial empty status is preserved in `baseline-status.txt`, paths in `inventory.txt`, and tracked Git object IDs/modes in `inventory-index.txt`. Detached worktree: `git worktree add --detach /tmp/daedalus-review-457d338 457d338ad9eec9a285a57436b37213b5ba577477`. Git reads in the temporary path used command-local `-c safe.directory=/tmp/daedalus-review-457d338`, not a global configuration change. Applicable ancestor/root instructions were searched; only `skills/AGENTS.md` applied. Concurrent original `review.md` was preserved.

The evidence tree contains source probes, raw reports and failure artifacts. It is temporary local storage, not a durable external artifact service. No image was pushed or signed; local build tags all use the review-specific prefix. All review services and process groups were stopped/removed after tests. Temporary worktree/venvs/local images and logs were retained for reproducibility. Source differences in the isolated tree are only the generated service worker and temporary reproduction files; the original source/locks remain unchanged.


## Python and native runtime

### Backend validation

Frozen checkout `/tmp/daedalus-review-457d338`, SHA `457d338ad9eec9a285a57436b37213b5ba577477`. Logs and repro sources are in `/tmp/daedalus-review-evidence-457d338/backend`. Production source, test sources and locks unchanged. All credentials below are disposable test values. Read Makefile, CI, Dockerfile, manifests, runtime check scripts and conftest before running. No real users, model providers, deployed databases or MCP services contacted.

Environment: host Python 3.12.3, isolated `/tmp/daedalus-review-evidence-457d338/backend/venv`; pytest9.1.1, coverage 7.16.0, pytest-cov7.1.0, pydantic2.13.5, redis8.1.0, HTTPX0.28.1, Requests2.34.2. Operations built `daedalus-review-457d338-backend:latest` from this checkout with pinned native NAT 1.8.0/Git3c44584ef5de2531e2ff548408f0e4658b755a69 and PyMilvus2.6.9. Exact image build evidence belongs to operations ledger.

For compact commands below, `E=/tmp/daedalus-review-evidence-457d338/backend` and `R=/tmp/daedalus-review-457d338`; variables are shell path shorthands, not environment modifications. Relative logs resolve below E.

| Check | Exact command / working directory | Result | Evidence and limits |
|---|---|---|---|
| Isolated environment | `uv venv --python /usr/bin/python3.12 /tmp/daedalus-review-evidence-457d338/backend/venv`; cwd checkout | Pass | `venv.log` |
| Development dependencies | `uv pip install --python /tmp/daedalus-review-evidence-457d338/backend/venv/bin/python -e '.[test]' redis`; cwd R/builder | Pass | `install.log`; no lock changes |
| Python suite/coverage | `$E/venv/bin/python -m pytest --cov --cov-report=xml:$E/coverage.xml --cov-report=term-missing --cov-fail-under=65 > $E/pytest-coverage.log 2>&1`; cwd R/builder | Pass:1327 passed,4 skipped,36.70 s;75.70% | `pytest-coverage.log`,`coverage.xml`; major service/framework modules mocked by conftest; percentage uses configured source list |
| Disposable Redis | `docker run --detach --name daedalus-review-457d338-python-redis --publish 127.0.0.1:16390:6379 redis/redis-stack-server:7.4.0-v8@sha256:798ab84d9f266936b034ab11c4d04a2b8e4b441884c5aa7d17ac951eefdf742a redis-stack-server --requirepass review-disposable-password` | Pass | Dedicated service; no shared production/other review database |
| Real Redis integration | `PYTEST_USE_REAL_REDIS=1 REDIS_URL=redis://default:review-disposable-password@127.0.0.1:16390 $E/venv/bin/python -m pytest -m integration -v > $E/pytest-integration.log 2>&1`; cwd R/builder | Pass:4 passed,1327 deselected,1.92 s | `pytest-integration.log`; approval identity/expiry/eight concurrent consumers, JSON/string data, autonomy lease/reclaim and action dedupe |
| Pinned native runtime contracts | `docker run --rm --network none --entrypoint python --mount type=bind,source=$R/builder/runtime_contract_check.py,target=/workspace/runtime_contract_check.py,readonly --mount type=bind,source=$R/builder/google_workspace_oauth_contract_check.py,target=/workspace/google_workspace_oauth_contract_check.py,readonly daedalus-review-457d338-backend:latest /workspace/runtime_contract_check.py > $E/runtime-contract.log 2>&1` | Pass | Native framework/graph/17 schemas/OAuth adapter and refresh contracts. Intentionally injected PermissionError logged as failure-case fixture; not test failure. No real remote provider |
| Catalog/skills/config | `docker run --rm --network none --entrypoint python --mount type=bind,source=$R/backend/tool-calling-config.yaml,target=/review-config.yaml,readonly daedalus-review-457d338-backend:latest /workspace/tool_catalog_contract_check.py --config /review-config.yaml --skills /skills > $E/tool-catalog-contract.log 2>&1` | Pass | 17 native tools,18 skills,102 resources |
| Approval false-success | `docker run --rm --network none --entrypoint python --mount type=bind,source=$E/repro_mcp_false_success.py,target=/workspace/repro.py,readonly daedalus-review-457d338-backend:latest /workspace/repro.py > $E/repro-mcp-false-success.log 2>&1` | Reproduced RV-003 | Exact executor/native failure serializer; remote failure injected |
| Sandbox ambiguous retry | `docker run --rm --network none --entrypoint python --mount type=bind,source=$E/repro_sandbox_retry.py,target=/workspace/repro.py,readonly daedalus-review-457d338-backend:latest /workspace/repro.py > $E/repro-sandbox-retry.log 2>&1` | Reproduced RV-008 | Real HTTPX/local HTTP fixture; one append POST creates two appends after 503 |
| Autonomy blocked cancel | `$E/venv/bin/python $E/repro_autonomy_cancel.py > $E/repro-autonomy-cancel.log 2>&1`; cwd R | Reproduced RV-013 | Real Requests/local SSE headers then silence; abort waits until data2 s later |
| Profile replace failure | `docker run --rm --network none --entrypoint python --mount type=bind,source=$E/repro_profile_replace.py,target=/workspace/repro.py,readonly daedalus-review-457d338-backend:latest /workspace/repro.py > $E/repro-profile-replace.log 2>&1` | Reproduced RV-007 | Native API/HTTPX localhost REST fixture; delete succeeds before retain503 |
| Forged tool terminal | `docker run --rm --network none --entrypoint python --mount type=bind,source=$E/repro_forged_approval_marker.py,target=/workspace/repro.py,readonly daedalus-review-457d338-backend:latest /workspace/repro.py > $E/repro-forged-approval-marker.log 2>&1` | Reproduced RV-017 | Native Langchain ordinary ToolMessage accepted by graph/HTTP detector without pending approval |
| Empty citation ledger | `docker run --rm --network none --entrypoint python --mount type=bind,source=$E/repro_citation_ledger.py,target=/workspace/repro.py,readonly daedalus-review-457d338-backend:latest /workspace/repro.py > $E/repro-citation-ledger.log 2>&1` | Reproduced RV-018 | Actual registered FunctionInfo schema/dispatcher, no fetching |
| Real Milvus fixture installation | `uv pip install --python $E/venv/bin/python --target $E/milvus-lite milvus-lite==2.5.1`; then `uv pip install --python $E/venv/bin/python --target $E/milvus-lite setuptools==80.9.0` | Pass | `install-milvus-lite.log`; fixture needs pkg_resources; target outside checkout |
| Milvus cutoff hypothesis | `docker run --rm --network none --entrypoint python --env PYTHONPATH=/review-milvus --mount type=bind,source=$E/milvus-lite,target=/review-milvus,readonly --mount type=bind,source=$E/repro_milvus_cutoff.py,target=/workspace/repro.py,readonly daedalus-review-457d338-backend:latest /workspace/repro.py > $E/repro-milvus-cutoff.log 2>&1` | Hypothesis disproved:original assertion failed | Actual temp MilvusLite DB/native PyMilvus Hit exposes .distance and retains matching result. Preserve failed suspect assertion; do not report cutoff bug. Earlier fixture import failed for missing pkg_resources until isolated setuptools installation |

Skipped/blocked: the4 default skips are the real Redis tests, subsequently run and passed. External full model/tool chat, real MCP consent/provider retries, full NV-Ingest/object-store/embedding/Milvus pipeline, Hindsight extraction/consolidation and sandbox runtime integration require unavailable disposable service implementations/configurations and were not run against live infrastructure. Failure fixtures demonstrate first-party request/outcome handling only. No performance benchmark; capacity concerns remain explicitly unmeasured. Static checks, container build/advisories, browser and Helm tests are recorded separately below.

Additional finding validation: `docker run --rm --network container:daedalus-review-457d338-python-redis --entrypoint python --mount type=bind,source=$E/repro_guarded_memory_clear.py,target=/workspace/repro.py,readonly daedalus-review-457d338-backend:latest /workspace/repro.py > $E/repro-guarded-memory-clear.log 2>&1` **PASS/reproduced RV-006**. The container shares only the dedicated review Redis container network namespace; script uses localhost Redis and a local Hindsight HTTP fixture, no external services. Native FunctionInfo and trusted RequestAttributes identity, genuine token issue/consume, real Redis cached brief before/after deletion; other user isolated. Seeded cache removed after reproduction.

Native runtime versions recorded by `docker run --rm --network none --entrypoint python daedalus-review-457d338-backend:latest -c 'import sys,importlib.metadata as m; print(sys.version); print({p:m.version(p) for p in ["nvidia-nat-core","pymilvus","httpx","requests","redis","langgraph","langchain-core"]})' > $E/native-versions.log 2>&1`: Python 3.12.3,NAT 1.8.0,PyMilvus2.6.9,HTTPX0.28.1,Requests2.34.2,Redis 4.6.0,Langgraph1.2.9,Langchain-core1.4.9. The ordinary host suite installed Redis 8.1.0; native Redis 4.6.0 exercised RV-006.

Milvus disproof confirmation: same Milvus fixture command as above with source script `$E/validate_milvus_cutoff.py`, log `$E/validate-milvus-cutoff.log`, **PASS**. Correct retained-match assertions confirm both searches return 1. Original suspect assertion/log preserved. This fixture embeds a fixed 2D vector for distance/type behavior only; it is not proof of deployed2048D embedding compatibility.

Cleanup completed: `docker rm --force daedalus-review-457d338-python-redis > $E/cleanup.log` **PASS**. Only the backend-owned disposable Redis container was removed. Review image, isolated venv, Milvus fixture target and evidence retained for traceability. All backend checks finished.


## Frontend and browser

### Frontend validation — frozen 457d338ad9eec9a285a57436b37213b5ba577477

All repository commands below used isolated checkout `/tmp/daedalus-review-457d338`; original checkout was not edited. Unless stated otherwise, working directory was `/tmp/daedalus-review-457d338/frontend`. Every Node command used `export PATH=/home/brandon/.nvm/versions/node/v22.17.0/bin:$PATH` first. Logs resolve relative to `/tmp/daedalus-review-evidence-457d338/frontend/`.

## Environment and safety boundaries

Node 22.17.0; npm 11.17.0; TypeScript 5.5.4; Next 15.5.25; React 18.3.1; Vitest 4.1.7; Playwright 1.61.1; ioredis5.10.1; sharp0.35.4; web-push3.6.7. Docker server29.8.0. Lockfile used unchanged; `npm ci --legacy-peer-deps` installed 941 packages in the disposable checkout. `Makefile`, CI commands, package scripts, e2e orchestration/compose/bootstrap, and build hooks were inspected before their execution. Existing application prompts/test fixtures were treated as input material, not reviewer instructions.

Disposable Compose project `daedalus-review-457d338` used only e2e Redis/SeaweedFS services and its own volume/network. Redis published127.0.0.1:16379, object service 127.0.0.1:18333; password/access keys are repository e2e test credentials. Frontend real-Redis integration used DB0, focused reproductions DB14, browser harness DB15. Browser app used loopback Next 15000/WebSocket15001/mock backend18000/control15099. The harness set test auth `e2e-user`, mock internal token, test session secret, object store bucket/prefix, worker concurrency2/lease10 s/heartbeat2 s/reclaim12 s/drain10 s. No deployment, live model/tool/OAuth service, real-user notification, or image publication was performed.

## Executed checks

| ID | Exact command (working directory frontend unless marked root) | Status | Result and evidence |
|---|---|---|---|
| frontend-install | `npm ci --legacy-peer-deps` | PASS | `npm-ci.log`;941 installed, lock unchanged. npm's all-dependency advisory summary reported9 (1 low/3 moderate/5 high); separate production audit below passed. Development-only applicability and primary-advisory triage are recorded below. |
| frontend-lint | `npm run lint` | PASS | `lint.log`; no lint errors. Existing configured rule exclusions preserved. |
| frontend-typecheck | `npx tsc --noEmit --incremental false` | PASS | `typecheck.log` empty because successful. |
| frontend-unit-coverage | `SESSION_SECRET=ci-session-secret-for-build-only npm run coverage` | PASS with opt-in skips | `coverage.log`;95 test files passed,5 skipped;818 tests passed,156 skipped. Statements46.22%, branches39.37%, functions40.58%, lines47.60%. `unit-coverage-final.json` retained. |
| disposable-services | root: `docker compose -p daedalus-review-457d338 -f frontend/e2e/docker-compose.yml up -d --build --wait --wait-timeout 120` | PASS | `disposable-services.log`; both disposable services healthy. Redis Dockerfile reviewed via operations workflow; no existing volume reused. |
| frontend-real-redis-integration | `REDIS_URL=redis://default:e2e-redis-password@127.0.0.1:16379 SESSION_SECRET=ci-session-secret-for-build-only npm run test:integration` | PASS | `redis-integration.log`;6 files177 tests,2.46 s; real RedisJSON plus tests exercising plain-string compatibility paths. |
| frontend-production-build | `SESSION_SECRET=e2e-session-secret-with-more-than-thirty-two-bytes NEXT_PUBLIC_WEBSOCKET_URL=ws://127.0.0.1:15001 NEXT_PUBLIC_WS_FALLBACK_POLL_INTERVAL_MS=1000 NEXT_TELEMETRY_DISABLED=1 npm run build` | PASS | `build.log`; Next production compilation/static generation/tracing and service-worker precache injection. Build changes isolated generated `public/sw.js`; no production source fix. |
| frontend-websocket-build | `npm run build:websocket` | PASS | `build-websocket.log`;463.8 KiB node22 bundle. |
| frontend-worker-build | `npm run build:stream-worker` | PASS | `build-worker.log`;1.9 MiB node22 bundle, sharp/web-push external. |
| browser-install | `npx playwright install chromium webkit` | PASS | `browser-install.log`; installed/available without error. |
| frontend-browser-matrix | `E2E_REDIS_URL=redis://default:e2e-redis-password@127.0.0.1:16379/15 NEXT_PUBLIC_WEBSOCKET_URL=ws://127.0.0.1:15001 NEXT_TELEMETRY_DISABLED=1 npx playwright test` | **FAIL initial run** | `browser.log`;150 passed,14 skipped,1failed,7.0min. The mobile-webkit test `ui-layout.spec.ts:364` timed out during login at `/login`, before keyboard assertions. All 5 projects attempted: desktop Chromium, mobile Chromium/WebKit, tablet WebKit,320px compact Chromium. |
| frontend-browser-failed-case-retry | `E2E_REDIS_URL=redis://default:e2e-redis-password@127.0.0.1:16379/15 NEXT_PUBLIC_WEBSOCKET_URL=ws://127.0.0.1:15001 NEXT_TELEMETRY_DISABLED=1 npx playwright test e2e/tests/ui-layout.spec.ts --project=mobile-webkit --grep 'mobile history drawer follows the keyboard viewport' --output=/tmp/daedalus-review-evidence-457d338/frontend/browser-retry-artifacts` | PASS | `browser-retry.log`;1 passed,3.4 s. Same build/services, new harness lifecycle. The original failure remains recorded; one retry does not prove flakiness cause or erase it. |
| frontend-production-audit | `npm run audit:production` | PASS | `audit.log`; no moderate-or-higher production dependency findings under repository policy. No application-security conclusion follows from this. |
| script-runner-attempt | `node --test scripts/check-production-audit.test.mjs scripts/mermaid-runtime.test.mjs` | FAIL — reviewer command error | `script-tests.log`; these tests import Vitest and require its runner. They are already included in the passing `npm run coverage` run. No source change or suppressed test. |
| focused-defense-regressions | `REDIS_URL=redis://default:e2e-redis-password@127.0.0.1:16379/14 SESSION_SECRET=review-session-secret DAEDALUS_INTERNAL_API_TOKEN=review-internal-test-token npx vitest run .review-repros/review.test.ts --reporter=verbose` | PASS reproducing defects | `reproductions-final.log`;7 assertions pass, including two real SSE ingestion terminal failures, nonowner finalization, removed credentials, body timeout, concurrent lost goals. These are temporary assertions of observed wrong behavior, not production regression tests expected to pass after remediation. Earlier5-test log retained as `reproductions.log`. |
| cross-user-http-boundary | `node .review-repros/cross-user-http.mjs` | PASS reproducing defect | `cross-user-http.log`;actual Next API→queue→worker run; owner read preserved access but content overwritten after attacker submission, despite attacker GET403. Real loopback test backend replaces model service. |
| svg-navigation-boundary | `node .review-repros/svg-browser.mjs` | PASS reproducing defect | `svg-browser.log`;actual Chromium navigation to authenticated storedSVG, harmless script reads only e2e user's `/api/auth/me`. Direct navigation only; owner isolation not bypassed. The first attempt failed401 because Secure cookie was not sent over test HTTP; final test manually adds only the same test sid without Secure for loopback. |
| disposable-cleanup | root: `docker compose -p daedalus-review-457d338 -f frontend/e2e/docker-compose.yml down --volumes` | PASS | `disposable-cleanup.log`; only review project's containers/network/volume removed. Playwright harness stopped its own child processes after each run. |

## Failure artifacts and limitations

Original failed browser case artifacts copied to:

- `/tmp/daedalus-review-evidence-457d338/frontend/browser-initial-artifacts/ui-layout-mobile-history-d-5984c-llows-the-keyboard-viewport-mobile-webkit/error-context.md`
- `/tmp/daedalus-review-evidence-457d338/frontend/browser-initial-artifacts/ui-layout-mobile-history-d-5984c-llows-the-keyboard-viewport-mobile-webkit/trace.zip`
- `/tmp/daedalus-review-evidence-457d338/frontend/browser-initial-artifacts/ui-layout-mobile-history-d-5984c-llows-the-keyboard-viewport-mobile-webkit/video.webm`

Browser14 skips are project-conditional platform/scenario skips; the original log records each skipped scenario. The separate daybook configuration was not invoked as a separate command; its test file is included by the default Chromium project's testDir. Ingestion/model/OAuth external behavior was controlled by local fixtures; these browser results do not prove pinned NAT compatibility. Python, Helm, container and scanner results are recorded in their sections below. No frontend checks remain running. No performance benchmark or physical-device/PWA installation experiment was run.

Source inspection status is recorded per file in `coverage.json`; the initially outstanding broad component/entry/hook/state review was subsequently completed, while the coverage ledger includes utility/config/documentation supplements and test bodies remain sampled where stated. Existing tests intentionally accept generic NAT clean EOF; the review separated that compatibility choice from the confirmed ingestion terminal-event defect. Tests that mock Redis/fetch are not counted as proof of external-service behavior; focused Redis and HTTP evidence explicitly identifies the real boundaries used.


## Deployment, image and security checks


- **helm-lint: PASS** (exit 0, 0.4 s), cwd `/tmp/daedalus-review-457d338`; command `helm lint helm/daedalus`; log `/tmp/daedalus-review-evidence-457d338/operations/helm-lint.log`.

- **helm-default: PASS** (exit 0, 0.4 s), cwd `/tmp/daedalus-review-457d338`; command `helm template daedalus helm/daedalus > /tmp/daedalus-review-evidence-457d338/operations/helm-default.yaml`; log `/tmp/daedalus-review-evidence-457d338/operations/helm-default.log`.

- **helm-custom: PASS** (exit 0, 0.2 s), cwd `/tmp/daedalus-review-457d338`; command `helm template daedalus helm/daedalus -f custom-values.yaml > /tmp/daedalus-review-evidence-457d338/operations/helm-custom.yaml`; log `/tmp/daedalus-review-evidence-457d338/operations/helm-custom.log`.

- **build-redis: PASS** (exit 0, 1.3 s), cwd `/tmp/daedalus-review-457d338`; command `docker build --provenance=mode=max --sbom=true -t daedalus-review-457d338-redis ./redis`; log `/tmp/daedalus-review-evidence-457d338/operations/build-redis.log`.

- **trivy-fs: PASS** (exit 0, 1.5 s), cwd `/tmp/daedalus-review-457d338`; command `trivy fs --scanners vuln --severity CRITICAL,HIGH --exit-code 1 --format json --output /tmp/daedalus-review-evidence-457d338/operations/trivy-fs.json .`; log `/tmp/daedalus-review-evidence-457d338/operations/trivy-fs.log`.

- **secret-scan: PASS** (exit 0, 4.2 s), cwd `/tmp/daedalus-review-457d338`; command `git -c safe.directory=/tmp/daedalus-review-457d338 log -1 --oneline && gitleaks detect --source . --verbose --redact --no-git --report-format json --report-path /tmp/daedalus-review-evidence-457d338/operations/gitleaks.json`; log `/tmp/daedalus-review-evidence-457d338/operations/secret-scan.log`.

- **compose-redis-auth: PASS** (exit 0, 1.0 s), cwd `/tmp/daedalus-review-457d338`; command `bash /tmp/daedalus-review-evidence-457d338/operations/compose-redis-auth-repro.sh`; log `/tmp/daedalus-review-evidence-457d338/operations/compose-redis-auth.log`.

- **compose-redis-network: FAIL** (exit 1, 1.9 s), cwd `/tmp/daedalus-review-457d338`; command `bash /tmp/daedalus-review-evidence-457d338/operations/compose-redis-network-repro.sh`; log `/tmp/daedalus-review-evidence-457d338/operations/compose-redis-network.log`.

- **trivy-inventory: PASS** (exit 0, 0.1 s), cwd `/tmp/daedalus-review-457d338`; command `trivy fs --scanners vuln --list-all-pkgs --format json --output /tmp/daedalus-review-evidence-457d338/operations/trivy-inventory.json frontend/package-lock.json && jq -e '[.Results[]? | select(.Target | endswith("package-lock.json"))] | length > 0' /tmp/daedalus-review-evidence-457d338/operations/trivy-inventory.json`; log `/tmp/daedalus-review-evidence-457d338/operations/trivy-inventory.log`.

- **npm-audit: PASS** (exit 0, 0.9 s), cwd `/tmp/daedalus-review-457d338/frontend`; command `PATH=/home/brandon/.nvm/versions/node/v22.17.0/bin:$PATH npm run audit:production`; log `/tmp/daedalus-review-evidence-457d338/operations/npm-audit.log`.

- **scan-redis: PASS** (exit 0, 6.1 s), cwd `/tmp/daedalus-review-457d338`; command `trivy image --severity CRITICAL,HIGH --exit-code 1 --format json --output /tmp/daedalus-review-evidence-457d338/operations/trivy-redis.json daedalus-review-457d338-redis`; log `/tmp/daedalus-review-evidence-457d338/operations/scan-redis.log`.

- **build-frontend: PASS** (exit 0, 77.5 s), cwd `/tmp/daedalus-review-457d338`; command `docker build --provenance=mode=max --sbom=true -t daedalus-review-457d338-frontend ./frontend`; log `/tmp/daedalus-review-evidence-457d338/operations/build-frontend.log`.

- **compose-config: PASS** (exit 0, 0.1 s), cwd `/tmp/daedalus-review-457d338`; command `docker compose --project-name daedalus-review-457d338 config --quiet`; log `/tmp/daedalus-review-evidence-457d338/operations/compose-config.log`.

- **build-backend: PASS** (exit 0, 138.3 s), cwd `/tmp/daedalus-review-457d338`; command `docker build --provenance=mode=max --sbom=true --build-context skills=./skills --target backend -t daedalus-review-457d338-backend ./builder`; log `/tmp/daedalus-review-evidence-457d338/operations/build-backend.log`.

- **scan-frontend: PASS** (exit 0, 1.8 s), cwd `/tmp/daedalus-review-457d338`; command `trivy image --severity CRITICAL,HIGH --exit-code 1 --format json --output /tmp/daedalus-review-evidence-457d338/operations/trivy-frontend.json daedalus-review-457d338-frontend`; log `/tmp/daedalus-review-evidence-457d338/operations/scan-frontend.log`.

- **scan-backend-release: FAIL** (exit 1, 5.1 s), cwd `/tmp/daedalus-review-457d338`; command `trivy image --severity CRITICAL,HIGH --exit-code 1 --format json --output /tmp/daedalus-review-evidence-457d338/operations/trivy-backend-release.json daedalus-review-457d338-backend`; log `/tmp/daedalus-review-evidence-457d338/operations/scan-backend-release.log`.

- **preflight-endpoints: PASS** (exit 0, 2.2 s), cwd `/tmp/daedalus-review-457d338`; command `python3 /tmp/daedalus-review-evidence-457d338/operations/preflight-endpoints-repro.py`; log `/tmp/daedalus-review-evidence-457d338/operations/preflight-endpoints.log`.

- **scan-backend: PASS** (exit 0, 37.8 s), cwd `/tmp/daedalus-review-457d338`; command `trivy image --severity CRITICAL,HIGH --exit-code 1 --skip-files '/workspace/.venv/lib/python3.12/site-packages/oci/golden_gate/models/create_azure_data_lake_storage_connection_details.py,/workspace/.venv/lib/python3.12/site-packages/oci/golden_gate/models/update_azure_data_lake_storage_connection_details.py' --format json --output /tmp/daedalus-review-evidence-457d338/operations/trivy-backend.json daedalus-review-457d338-backend`; log `/tmp/daedalus-review-evidence-457d338/operations/scan-backend.log`.

- **secret-history: PASS** (exit 0, 4.5 s), cwd `/tmp/daedalus-review-457d338`; command `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=safe.directory GIT_CONFIG_VALUE_0=/tmp/daedalus-review-457d338 gitleaks detect --source . --verbose --redact --report-format json --report-path /tmp/daedalus-review-evidence-457d338/operations/gitleaks-history.json`; log `/tmp/daedalus-review-evidence-457d338/operations/secret-history.log`.

- **script-syntax: PASS** (exit 0, 0.0 s), cwd `/tmp/daedalus-review-457d338`; command `bash -n deploy.sh && bash -n scripts/test_redis_helm_upgrade.sh && python3 -c "import ast,pathlib; [ast.parse(p.read_text()) for p in pathlib.Path(\"scripts\").glob(\"*.py\")]; print(\"scripts parsed\")"`; log `/tmp/daedalus-review-evidence-457d338/operations/script-syntax.log`.

- **scan-backend-release-retry: FAIL** (exit 1, 33.5 s), cwd `/tmp/daedalus-review-457d338`; command `trivy image --severity CRITICAL,HIGH --exit-code 1 --format json --output /tmp/daedalus-review-evidence-457d338/operations/trivy-backend-release.json daedalus-review-457d338-backend`; log `/tmp/daedalus-review-evidence-457d338/operations/scan-backend-release-retry.log`.

- **compose-redis-controls: FAIL** (exit 1, 0.9 s), cwd `/tmp/daedalus-review-457d338`; command `bash /tmp/daedalus-review-evidence-457d338/operations/compose-redis-controls.sh`; log `/tmp/daedalus-review-evidence-457d338/operations/compose-redis-controls.log`.

**Compose preliminary probe interpretation:** The first auth PASS only demonstrates loopback authentication; the network FAIL demonstrates protected-mode rejection. The controls FAIL tries a health check on the default-user container after its startup failure. The later compose-redis-exact PASS means both expected defects were asserted against the shipped YAML command and is the authoritative reproduction.

**Preflight experiment disproof:** Initial stdout incorrectly concludes routing from Ready endpoints. `preflight-endpoints.json` records `ports: null` on both slices because Services use named targetPort=http and probes declare no matching ports. It proves selector overlap, not diverted traffic. No confirmed finding is reported from this experiment. Backend pod discovery also health-probes candidate IPs.

- **redis-upgrade: PASS** (exit 0, 226.5 s), cwd `/tmp/daedalus-review-457d338`; command `KIND_CLUSTER_NAME=daedalus-review-457d338 KIND_POD_SUBNET=10.248.0.0/16 KIND_SERVICE_SUBNET=10.249.0.0/16 bash /tmp/daedalus-review-evidence-457d338/operations/test_redis_helm_upgrade-isolated.sh`; log `/tmp/daedalus-review-evidence-457d338/operations/redis-upgrade.log`.

- **compose-redis-exact: PASS** (exit 0, 2.2 s), cwd `/tmp/daedalus-review-457d338`; command `python3 /tmp/daedalus-review-evidence-457d338/operations/compose-redis-exact-repro.py`; log `/tmp/daedalus-review-evidence-457d338/operations/compose-redis-exact.log`.

- **nginx-default: PASS** (exit 0, 2.1 s), cwd `/tmp/daedalus-review-457d338`; command `docker run --rm --network none --add-host daedalus-frontend.default.svc.cluster.local:127.0.0.1 --add-host daedalus-backend-default.default.svc.cluster.local:127.0.0.1 -v /tmp/daedalus-review-evidence-457d338/operations/nginx-default.conf:/etc/nginx/conf.d/default.conf:ro --entrypoint nginx nginxinc/nginx-unprivileged:alpine@sha256:a718212f9cf21e241f14067333000a3f0930292f5354fe0db269e9a2a2596b9e -t`; log `/tmp/daedalus-review-evidence-457d338/operations/nginx-default.log`.

- **nginx-custom: FAIL** (exit 1, 0.2 s), cwd `/tmp/daedalus-review-457d338`; command `docker run --rm --network none --add-host daedalus-frontend.default.svc.cluster.local:127.0.0.1 --add-host daedalus-backend-default.default.svc.cluster.local:127.0.0.1 -v /tmp/daedalus-review-evidence-457d338/operations/nginx-custom.conf:/etc/nginx/conf.d/default.conf:ro -v /tmp/daedalus-review-evidence-457d338/operations/tls.crt:/etc/nginx/ssl/tls.crt:ro -v /tmp/daedalus-review-evidence-457d338/operations/tls.key:/etc/nginx/ssl/tls.key:ro --entrypoint nginx nginxinc/nginx-unprivileged:alpine@sha256:a718212f9cf21e241f14067333000a3f0930292f5354fe0db269e9a2a2596b9e -t`; log `/tmp/daedalus-review-evidence-457d338/operations/nginx-custom.log`.

- **nginx-compose: PASS** (exit 0, 2.0 s), cwd `/tmp/daedalus-review-457d338`; command `docker run --rm --network none --add-host frontend:127.0.0.1 --add-host backend:127.0.0.1 -v /tmp/daedalus-review-457d338/nginx/conf.d/frontend.conf:/etc/nginx/conf.d/default.conf:ro --entrypoint nginx nginx:alpine@sha256:7068961d45b07b2af510ac002e9daa63a1d3eba2111202d6768798690800fffd -t`; log `/tmp/daedalus-review-evidence-457d338/operations/nginx-compose.log`.

- **nginx-custom-retry: PASS** (exit 0, 0.3 s), cwd `/tmp/daedalus-review-457d338`; command `docker run --rm --network none --add-host daedalus-frontend.default.svc.cluster.local:127.0.0.1 --add-host daedalus-backend-default.default.svc.cluster.local:127.0.0.1 -v /tmp/daedalus-review-evidence-457d338/operations/nginx-custom.conf:/etc/nginx/conf.d/default.conf:ro -v /tmp/daedalus-review-evidence-457d338/operations/tls.crt:/etc/nginx/ssl/tls.crt:ro -v /tmp/daedalus-review-evidence-457d338/operations/tls.key:/etc/nginx/ssl/tls.key:ro --entrypoint nginx nginxinc/nginx-unprivileged:alpine@sha256:a718212f9cf21e241f14067333000a3f0930292f5354fe0db269e9a2a2596b9e -t`; log `/tmp/daedalus-review-evidence-457d338/operations/nginx-custom-retry.log`.

- **scan-nginx-helm: FAIL** (exit 1, 0.8 s), cwd `/tmp/daedalus-review-457d338`; command `trivy image --scanners vuln --severity CRITICAL,HIGH --exit-code 1 --format json --output /tmp/daedalus-review-evidence-457d338/operations/trivy-nginx-helm.json nginxinc/nginx-unprivileged:alpine@sha256:a718212f9cf21e241f14067333000a3f0930292f5354fe0db269e9a2a2596b9e`; log `/tmp/daedalus-review-evidence-457d338/operations/scan-nginx-helm.log`.

- **scan-nginx-compose: FAIL** (exit 1, 1.0 s), cwd `/tmp/daedalus-review-457d338`; command `trivy image --scanners vuln --severity CRITICAL,HIGH --exit-code 1 --format json --output /tmp/daedalus-review-evidence-457d338/operations/trivy-nginx-compose.json nginx:alpine@sha256:7068961d45b07b2af510ac002e9daa63a1d3eba2111202d6768798690800fffd`; log `/tmp/daedalus-review-evidence-457d338/operations/scan-nginx-compose.log`.

- **runtime-versions: PASS** (exit 0, 1.4 s), cwd `/tmp/daedalus-review-457d338`; command `python3.12 --version; /home/brandon/.nvm/versions/node/v22.17.0/bin/node --version; /home/brandon/.nvm/versions/node/v22.17.0/bin/npm --version; docker version --format "{{.Client.Version}} {{.Server.Version}}"; docker compose version; helm version --short; kind version; kubectl version --client=true -o json; uv --version; trivy --version; docker run --rm --network none --entrypoint python daedalus-review-457d338-backend -c "import platform,importlib.metadata as m; print(platform.python_version()); print({n:m.version(n) for n in [\"nvidia-nat-core\",\"pydantic\",\"fastapi\",\"starlette\",\"cryptography\",\"urllib3\",\"oci\"]})"; docker run --rm --network none --entrypoint node daedalus-review-457d338-frontend --version; docker run --rm --network none --entrypoint redis-server daedalus-review-457d338-redis --version`; log `/tmp/daedalus-review-evidence-457d338/operations/runtime-versions.log`.

## Environment and interpretation

Review checkout `/tmp/daedalus-review-457d338`, frozen source `457d338ad9eec9a285a57436b37213b5ba577477`. Root Compose was validated only with an ephemeral `.env` copied from the committed template and fake auth/session values, memory disabled; removed immediately after config check. No original .env/credentials were read. All containers had unique review names/tags; reproductions used `--network none` or a disposable internal Docker network. Redis Helm test used only Kind cluster `daedalus-review-457d338`, explicit temporary kubeconfig `/tmp/daedalus-redis-upgrade.ankEMM/kubeconfig`, pod 10.248/16 and service 10.249/16 networks; it completed10 revisions including compatible rollback and then removed its cluster. The fixture script was copied into evidence solely to override hardcoded repo path/image name for isolation; assertions and security settings were retained.

Backend image build executed the real pinned NAT 1.8.0 runtime contract against Git commit3c44584ef5de2531e2ff548408f0e4658b755a69, native plugin registration and framework adapters. It included local/fake transports for external OAuth/provider behavior, not actual Google/model/Milvus proof. Images used cached build layers where Docker matched immutable inputs; vulnerability databases were current.

Trivy0.74.0 vulnerability DB updated2026-09-11 19:05:14 UTC and downloaded21:12 UTC. Filesystem scan inventoried both240-package runtime locks, builder's26-package test lock, and frontend's403-package production graph. The warning `uv lockfile must contain 1 root package` concerns the root uv.lock, which parses as a zero-package stub; it did not omit either runtime lock. Runtime image scan inventoried302 OS+252 Python packages (backend),19 OS+260 Node packages (frontend),130 OS packages (Redis). Redis Stack server/modules are copied/vendor binaries and not a package-manager dependency graph; an empty OS CVE scan does not independently cover their full advisory history. Alpine3.24.1 was detected, with scanner warning that3.24 is absent from its EOL list. npm's production audit returned no moderate-or-higher findings. Those three image scans and lockfiles returned no HIGH/CRITICAL CVEs. Expanded scans of pinned nginx (two variants) and SeaweedFS failed with 11/11/16 HIGH package rows; current primary advisory verification, affected version ranges, severity differences, non-applicable components and remaining reachability gaps are recorded in `operations/advisory_review.md`.

Completed secret scan covers 537 Git commits, plus a separate working-tree scan. Existing three .gitleaksignore historical example fingerprints were retained. CI-equivalent runtime scans passed using the existing two-file OCI example exclusion; release-equivalent scan failed on the same public vendor examples (RV-023). An initial simultaneous Trivy scan failed on a cache lock and was repeated sequentially; its final result is the substantive release-gate failure.

Custom nginx initial fixture failed because its temporary TLS key was mode0600 and unreadable to nginx UID101. Retried after changing only the disposable key to match Kubernetes Secret file readability; successful `nginx -t`. The deprecated nginx `listen ... http2` warning is a low-impact compatibility warning, not a present startup failure.

No deployed application, real external integration, production credential, user interaction, image publishing/signing, registry write, or live Kubernetes cluster was exercised. Root Compose full-stack readiness is blocked by RV-004; external model/MCP/RAG calls were deliberately not attempted. No performance claims are based solely on inspection.

- **scan-seaweed: FAIL** (exit 1, 2.7 s), cwd `/tmp/daedalus-review-457d338`; command `trivy image --scanners vuln --severity CRITICAL,HIGH --exit-code 1 --format json --output /tmp/daedalus-review-evidence-457d338/operations/trivy-seaweed.json chrislusf/seaweedfs:4.39@sha256:c7d6c721b30ae711db766bbbfd40192776e263d4e51e22f57baef7bef93c12c6`; log `/tmp/daedalus-review-evidence-457d338/operations/scan-seaweed.log`.

- **nginx-package-reachability: PASS** (exit 0, 0.3 s), cwd `/tmp/daedalus-review-457d338`; command `docker run --rm --network none --entrypoint sh nginxinc/nginx-unprivileged:alpine@sha256:a718212f9cf21e241f14067333000a3f0930292f5354fe0db269e9a2a2596b9e -c 'id; apk info -L libuuid; command -v mount; command -v nsenter; ls -l /bin/mount /usr/bin/nsenter; nginx -V 2>&1'`; log `/tmp/daedalus-review-evidence-457d338/operations/nginx-package-reachability.log`.

- **primary-advisories: PASS** (exit 0, 1.6 s), cwd `/tmp/daedalus-review-457d338`; command `python3 /tmp/daedalus-review-evidence-457d338/operations/verify_advisories.py`; log `/tmp/daedalus-review-evidence-457d338/operations/primary-advisories.log`.

Runtime/tool versions: host Python 3.12.3; test Node 22.17.0 with npm 11.17.0; Docker client/server29.8.0; Compose5.5.1; Helm3.17.2; Kind0.26.0 (disposable node1.32.0); kubectl1.35.3; uv0.10.4 host (container build uv0.9.15); Trivy0.74.0. Backend Python 3.12.3/NATcore1.8.0/Pydantic2.13.4/FastAPI0.139.2/Starlette1.3.1/OCI2.182.0; frontend container Node 22.23.1; Redis server7.4.7 (Stack compatibility7.4.0-v8); pinned nginx1.31.3/OpenSSL3.5.7. Native version log's npm-only command accidentally resolves default host Node 18.19.1 and warns; actual npm audit explicitly prepended Node 22 to PATH and passed. This is an inspection-command artifact, not a project runtime failure.

Advisory HTTP retrieval succeeded for24 primary source URLs, but Apache's HTML was only its JavaScript shell and web retrieval returned403; that one advisory remains unverified. An initial HTML parsing attempt failed because host BeautifulSoup was absent; repeated with Python stdlib HTMLParser, without dependency installation. No exploit claims depend on that initial parser attempt.


## Skills and root checks

### Root validation

CWD /tmp/daedalus-review-457d338 unless stated. All static helpers inspected first; no live operational helper invoked.

- `python3 scripts/check_skill_links.py > /tmp/daedalus-review-evidence-457d338/root/skill-links.log 2>&1`: PASS. All local skill file/anchor links resolve.
- Python AST parsing all `skills/**/*.py`; `bash -n` all `skills/**/*.sh`: PASS; root/skill-syntax.log. Source syntax, not execution proof.
- CWD builder: `/tmp/daedalus-review-evidence-457d338/backend/venv/bin/python -m pytest -q -s tests/review_probe_skills.py`: PASS 2 defect-confirming temporary probes, Python 3.12.3/pytest9.1.1. root/skill-reproductions.log. Repository conftest framework mocks; subprocess real.
- `docker run --rm --network none --entrypoint python --mount type=bind,src=/tmp/daedalus-review-evidence-457d338/root/runtime_skill_probe.py,dst=/tmp/review_probe.py,readonly daedalus-review-457d338-backend:latest /tmp/review_probe.py`: first harness FAIL (incorrect keyword call into actual FunctionInfo single_fn), then PASS after using actual typed schema. root/runtime-skill-reproductions.log and root/runtime-skill-reproductions-second.log. Same source, NAT 1.8 exact image ID sha256:c7cf6f5dfd3a6d47241ab457942b774ca01031669df433e5569160d507c8f27c; no network/credentials; own child reaped. Source root/runtime_skill_probe.py.


## Additional read-only static checks and fixture verification

Working directory for these checks: `/tmp/daedalus-review-457d338`. Isolated tool environment `/tmp/daedalus-review-evidence-457d338/root/static-venv` created using `uv venv --python 3.12 /tmp/daedalus-review-evidence-457d338/root/static-venv`; installed `ruff==0.6.4`, `bandit[toml]==1.7.9`, `isort==5.13.2` with `uv pip install --python /tmp/daedalus-review-evidence-457d338/root/static-venv/bin/python ruff==0.6.4 'bandit[toml]==1.7.9' isort==5.13.2`; installation log `root/static-install.log`.

The exact expanded argv, cwd, duration, exit and log for each static invocation are retained in `root/static-checks.json`. File arguments are all tracked `.py` paths from `git -c safe.directory=/tmp/daedalus-review-457d338 ls-files -z`; Bandit excludes `/tests/` consistently with the hook. No temporary probe is in that tracked list.

| Exact command form (P = isolated static-venv/bin, tracked paths expanded in static-checks.json) | Status | Evidence |
|---|---|---|
| `$P/ruff check <tracked .py paths>` | PASS | `root/ruff-check.log` |
| `$P/ruff format --check <tracked .py paths>` | FAIL | `root/ruff-format.log`;1 would-reformat worker.py,163 already formatted |
| `$P/isort --check-only --profile black -o redis <tracked .py paths>` | FAIL | `root/isort-check.log`;worker.py import formatting |
| `$P/bandit -c pyproject.toml -f json -o /tmp/daedalus-review-evidence-457d338/root/bandit.json <tracked non-test .py paths>` | PASS | `root/bandit.log`,`root/bandit.json` |

Blanket pre-commit was not run because several hooks mutate files (`--fix`, `--write`, formatters). Its security/syntax/lint/link intentions were covered with non-mutating checks where relevant; no claim is made that every pre-commit hook passed. These style failures are validation results, not prioritized behavioral findings.

Skill syntax used `ast.parse(p.read_text())` for each `skills/**/*.py` and `bash -n <path>` for each `skills/**/*.sh`; `root/skill-syntax.log` records results. Local links use the exact standalone command in the root section. Fixture validation used Python `gzip.decompress`, `json.loads` and `hashlib.sha256` on `test-fixtures/code-audit-independent-20260909.json.gz`: PASS,1,752,709 bytes and README-matching SHA256 `854e10f07cc7904c2d38ca113166408e6fbe59fc120e4947f8bf86b35ea16865`; `root/fixture-integrity.log`.

Final source-material verification: `python3 /tmp/daedalus-review-evidence-457d338/root/verify_source_material.py > /tmp/daedalus-review-evidence-457d338/root/source-material-final.log 2>&1`, cwd `/tmp/daedalus-review-457d338`, Python 3.12.3: **PASS**. The retained script gives the exact earlier AST/Bash/fixture operations: 10 Python skill helpers parsed, 9 Bash helpers syntax-checked, and the frozen JSON fixture size/hash reverified. This repeat closes command traceability; it adds no external-service evidence.

## Primary advisory and applicability verification

### Current primary advisory verification

Reviewed on 2026-09-11. Trivy 0.74.0 DB UpdatedAt `2026-09-11T19:05:14.711544808Z`, downloaded `21:12:00.654710444Z`. Exact image scans are listed above. Bare evidence paths in this advisory subsection resolve under the operations evidence directory. Scanner results are affected-package candidates, not demonstrated reachable exploits. Repeated libcrypto/libssl rows represent one advisory, and duplicated nginx image results are not independent defects. No production exploit was attempted.

The three locally built application/Redis images and resolved lockfile graphs reported zero HIGH/CRITICAL CVEs. Expanding beyond CI's image list found 11 HIGH package rows in each pinned nginx image and 16 in the pinned SeaweedFS image. Primary sources substantially narrow that raw severity interpretation.

| Resolved component | Scanner candidate(s) | Primary verification | Reachability/disposition |
|---|---|---|---|
| SeaweedFS binary `google.golang.org/grpc v1.81.1` | CVE-2026-84304 | [Maintainer advisory](https://github.com/grpc/grpc-go/security/advisories/GHSA-vp52-pcj8-j9qc) confirms versions through 1.83.0; fixed 1.83.1. | Affected dependency confirmed. gRPC HTTP/2 fragmented DATA can exhaust heap. Compose does not publish Seaweed's gRPC ports to the host; attacker access to its internal service network is a required condition. No exploit or application-route reachability reproduction. |
| Same grpc version | GHSA-hrxh-6v49-42gf | [Maintainer advisory](https://github.com/grpc/grpc-go/security/advisories/GHSA-hrxh-6v49-42gf) confirms versions below 1.82.1. | Affected dependency; HTTP/2 rapid-reset handling is relevant to gRPC transports. Separate xDS policy flaws require xDS configuration, which was not established. |
| Same grpc version | CVE-2026-84445 | [Maintainer advisory](https://github.com/grpc/grpc-go/security/advisories/GHSA-2v4p-qf9q-27wj) fixes 1.82.2/1.83.2. | Requires `xds.NewGRPCServer`; not established in this deployment. Do not report a confirmed remotely reachable crash. |
| Seaweed Go stdlib v1.25.12 | CVE-2026-33818 | [Go advisory GO-2026-5972](https://pkg.go.dev/vuln/GO-2026-5972), fixed 1.25.13/1.26.6. | Affected stdlib; ASN.1 recursion requires attacker-controlled data entering affected decoder. Not demonstrated. |
| Same Go version | CVE-2026-39821 | [Go advisory GO-2026-5026](https://pkg.go.dev/vuln/GO-2026-5026), fixed 1.25.13/1.26.6. | IDNA hostname interpretation requires a security decision using the affected conversion; not demonstrated. |
| Same Go version | CVE-2026-56853 | [Go advisory GO-2026-6089](https://pkg.go.dev/vuln/GO-2026-6089), fixed 1.25.13/1.26.6. | Requires stdlib unencrypted HTTP/2 preface handling; service configuration not proven to use it. |
| Same Go version | CVE-2026-56858 | [Go advisory GO-2026-6091](https://pkg.go.dev/vuln/GO-2026-6091), fixed 1.25.13/1.26.6. | Requires attacker input in affected HTML template context; not demonstrated. |
| Same Go version | CVE-2026-56859 | [Go advisory GO-2026-6088](https://pkg.go.dev/vuln/GO-2026-6088), fixed 1.25.13/1.26.6. | Affected XML decoder dependency; no crafted S3 XML request sent to show stack exhaustion. |
| Same Go version | CVE-2026-56860 | [Go advisory GO-2026-6218](https://pkg.go.dev/vuln/GO-2026-6218), fixed 1.25.13/1.26.6. | Requires affected relative URL resolution with attacker-controlled parent segments; not demonstrated. |
| Same Go version | CVE-2026-56862 | [Go advisory GO-2026-6090](https://pkg.go.dev/vuln/GO-2026-6090), fixed 1.25.13/1.26.6. | Go TLS KeyUpdate issue. Shipped object-store uses HTTP; no vulnerable TLS server configuration established. |
| Seaweed `golang.org/x/crypto v0.53.0` | CVE-2026-56854 | [Go advisory GO-2026-6303](https://pkg.go.dev/vuln/GO-2026-6303), fixed 0.55.0. | SSH source-address callback checks; no SSH server configured. |
| Seaweed `golang.org/x/image v0.43.0` | CVE-2026-46603 | [Go advisory GO-2026-6222](https://pkg.go.dev/vuln/GO-2026-6222), fixed 0.45.0. | VP8L allocation exhaustion; needs attacker image decoded by affected path. No reachability reproduction. |
| Seaweed `golang.org/x/text v0.38.0` | CVE-2026-56852 | [Go advisory GO-2026-5970](https://pkg.go.dev/vuln/GO-2026-5970), fixed 0.39.0. | Unicode normalization iteration with invalid UTF-8; no relevant call path proved. |
| Seaweed Apache Thrift pseudo-version `v0.23.1-0.20260429145742-d2acd3c49e58` | CVE-2026-43871 | Scanner suggests 0.24.0. [Apache source URL](https://lists.apache.org/thread/7v3jhgwfbmhx42424phydlnzb109g8b9) returned a browser shell to urllib and 403 to web retrieval, not readable advisory content. | **Unverified concern only.** Do not treat the scanner's pseudo-version range match as an independently verified vulnerability. |
| All three upstream images: OpenSSL libcrypto/libssl 3.5.7-r0 | CVE-2026-14456 | [OpenSSL advisory](https://openssl-library.org/news/secadv/20260813.txt) confirms affected 3.5 branch and fix 3.5.8, but assigns **Low**, whereas the scanner labels HIGH. | Requires OpenSSL QUIC listener. No QUIC listeners are configured in shipped nginx configs; Seaweed uses Go TLS/plain HTTP. Not a demonstrated app vulnerability and not a HIGH finding here. |
| Both nginx: Expat 2.8.2-r0 | CVE-2026-76956 / CVE-2026-76957 | Maintainer fixes [1326](https://github.com/libexpat/libexpat/pull/1326), [1322](https://github.com/libexpat/libexpat/pull/1322); [2.8.4 release history](https://github.com/libexpat/libexpat/blob/R_2_8_4/expat/Changes). | First requires getentropy-only entropy path (maintainer notes getrandom and other paths unaffected); second requires custom encoding callback reentrancy. No configured nginx path matching either established. |
| Both nginx: libuuid 2.42.1-r0 (util-linux source package) | CVE-2026-53612 / 53613 / 53614 / 76642 / 78408 / 78409 / 78410 | All seven upstream util-linux security advisory bodies fetched and reviewed; URLs and snapshots in `advisories/index.json`. They concern util-linux mount/libmount/nsenter. | **Component mismatch for the reviewed nginx image:** `apk info -L libuuid` contains only two shared-library paths; `/bin/mount` and `/usr/bin/nsenter` are BusyBox symlinks. These are not evidence of seven exploitable libuuid bugs. `nginx-package-reachability.log` records inspection under `--network none`. |

Trivy also warned that CVE-2026-80256 details were missing from its DB (possibly rejected); it is not included as a verified finding. Alpine 3.24.1 is not present in Trivy's EOL table; this warning limits lifecycle classification, not the package version extraction.

Primary fetch index and read excerpts are retained in `advisories/index.json`, `advisories/verified.json`, and `primary-advisories.log`. Source bodies were treated solely as review evidence. Public fetches had no credentials. The focused confirmed report issue is the omitted runtime image scan inventory and affected dependencies, not a claim that all scanner rows are exploitable.


### Frontend development dependency advisory triage

Reviewed 2026-09-11 against frozen SHA `457d338ad9eec9a285a57436b37213b5ba577477`. The full audit reports **9 affected package records:1 low,3 moderate,5 high**, representing **17 distinct advisories** (transitive effects and duplicate records are not17 demonstrated exploits). Every reported installed lock node is `dev:true`. A fresh `npm audit --omit=dev --json` reports0 vulnerabilities. The exact affected package paths are absent from the built `.next/standalone` directory. This supports development/build/test applicability; it does not prove there are no vulnerabilities in production or in Node's separately bundled libraries.

**Evidence label:** Code-supported affected dependency versions, verified against current maintainer advisories/patches and resolved lock/install paths. **Exploit status:** No exploit probes performed; no remotely reachable production exploit demonstrated. There are actionable development dependency updates, with lower deployment impact than the confirmed application defects.

| Affected audit package(s), installed version | Resolved caller and prerequisite | Current primary advisory / fixed minimum for installed major | Applicability and remediation direction |
|---|---|---|---|
| `vitest`4.1.7 and `@vitest/mocker`4.1.7 (2 moderate records) | Vitest tests use `environment: 'jsdom'`; no public `mockerPlugin`, standalone `interceptorPlugin`, or Vitest browser mode is configured in first-party source. Advisory requires accessible mock registration on a dev-server socket, or authenticated browser RPC. | [GHSA-82fw-gwwq-j7x9](https://github.com/vitest-dev/vitest/security/advisories/GHSA-82fw-gwwq-j7x9): affected≥2.1.0,<4.1.11; fixed 4.1.11. | Affected installed test tools; required remote registration surface not demonstrated in this configuration. Upgrade Vitest/mocker and matching coverage package together, then rerun unit/coverage tests. |
| `autoprefixer`10.5.0 (1 high transitive-effect record) | Direct dev dependency, used by `postcss.config.js`; severity inherited from its Browserslist subtree. | No separate Autoprefixer advisory in this audit; see next two rows. | Count this as the same dependency-chain exposure, not a distinct exploit/root cause. Resolve compatible current Browserslist/baseline patches. **Do not blindly apply npm's suggested Autoprefixer5.2.0 downgrade**; that suggestion is a major backward change. |
| `browserslist`4.28.2 (1 high record) | Autoprefixer→Browserslist. Distinct attacker-controlled query results can grow its cache; untrusted discovered `browserslist-stats.json`/custom stats can crash or alter a normalization object's prototype. No application route passes user input into these APIs. | [cache growth](https://github.com/browserslist/browserslist/security/advisories/GHSA-c83g-rgw3-j3cx) and [stats normalization](https://github.com/browserslist/browserslist/security/advisories/GHSA-73wf-gq98-2v4g): both affected≤4.28.6; fixed 4.28.7. | Build/development configuration exposure. Malicious repository/config inputs are the relevant prerequisite, rather than chat/upload requests. Upgrade compatible transitive version and rerun CSS/build checks. |
| `baseline-browser-mapping`2.10.24 (1 moderate record) | Autoprefixer→Browserslist→baseline mapping. Invalid/conflicting parameters can call `process.exit()`; repository uses build-managed browser configuration. | [maintainer fix](https://github.com/web-platform-dx/baseline-browser-mapping/commit/de733e2d8959559f7bb255d5927f3afcb6f31589) explicitly replaces exit calls with thrown errors; [release2.11.0](https://github.com/web-platform-dx/baseline-browser-mapping/releases/tag/v2.11.0), affected≥2.0.0,<2.11.0. [Advisory](https://github.com/advisories/GHSA-w5vr-8v7q-w6rv). | Build/test process availability if invalid input reaches mapping; no application request path found. Upgrade compatible baseline mapping with the Browserslist chain. |
| `brace-expansion`1.1.14 and 5.0.6 (1 high record,2 installed nodes) | ESLint→minimatch3.1.5→1.1.14; eslint-config-next→TypeScript ESLint parser→minimatch10.2.5→5.0.6. Crafted brace/glob patterns cause excessive expansion, CPU or memory use. | [exponential expansion](https://github.com/juliangruber/brace-expansion/security/advisories/GHSA-3jxr-9vmj-r5cp): fixes 1.1.16/5.0.7; [unbounded output](https://github.com/juliangruber/brace-expansion/security/advisories/GHSA-mh99-v99m-4gvg):1.1.17/5.0.8; [intermediate-array bypass](https://github.com/juliangruber/brace-expansion/security/advisories/GHSA-rgw5-rvv9-x895):1.1.18/5.0.9. Both installed versions affected by all three. | Development lint/config glob processing; no runtime API imports. Upgrade each installed major to at least1.1.18/5.0.9 through compatible parent updates and rerun lint/type checks. Do not stop at the superseded first patch. |
| `js-yaml`4.1.1 (1 high record) | ESLint/@eslint/eslintrc configuration parser; first-party application source has no js-yaml import. Crafted YAML merge/ordered-map structures cause CPU exhaustion. | [repeated aliases](https://github.com/nodeca/js-yaml/security/advisories/GHSA-h67p-54hq-rp68):4.2.0; [merge chains](https://github.com/nodeca/js-yaml/security/advisories/GHSA-52cp-r559-cp3m):4.3.0; [ordered-map resolution](https://github.com/nodeca/js-yaml/security/advisories/GHSA-5p4m-2wfm-xmqj):4.3.1; [empty merge sources](https://github.com/nodeca/js-yaml/security/advisories/GHSA-2883-xcg3-v3hh):4.3.2. Installed4.1.1 falls in all affected ranges. | Development YAML/config processing. Root override `^4.1.0` permits a4.3.2 update; resolve/test deliberately. These are the JavaScript package's advisories, not Python PyYAML or builder skill parsing findings. |
| `postcss-selector-parser`6.1.2 (1 low record,2 installed nodes) | TailwindCSS3.4.19 directly and via postcss-nested6.2.0; recursive CSS AST processing. Separate typography dependency6.0.10 is not in this advisory's listed affected range. | [maintainer recursion guard patch](https://github.com/postcss/postcss-selector-parser/commit/5bc698cef66f8abd12610dc623e5d67cbc0f869d), [6.1.3 release](https://github.com/postcss/postcss-selector-parser/releases/tag/6.1.3); [GHSA-w9m9-85wc-3x92](https://github.com/advisories/GHSA-w9m9-85wc-3x92) lists≥6.1.0,<6.1.3. | Build CSS handling, not a deployed user-CSS compiler in this checkout. Upgrade compatible6.x transitive dependency and verify CSS generation. |
| `undici`7.28.0 (1 high record) | jsdom29.1.1 test environment. Advisories require explicit retry/cache interceptors, unsafe custom blob-like bodies, or attacker-controlled cookie-domain/unparsed fields. jsdom dispatcher and first-party sources inspected; no configured application cache/retry interceptor or direct affected-cookie API path found. | All five fixed 7.29.0: [retry framing](https://github.com/nodejs/undici/security/advisories/GHSA-8xcm-r25x-g524), [private cache directives](https://github.com/nodejs/undici/security/advisories/GHSA-4cwx-7wf7-3272), [blob type injection](https://github.com/nodejs/undici/security/advisories/GHSA-m8rv-5g2x-5cg5), [cache whitespace bypass](https://github.com/nodejs/undici/security/advisories/GHSA-jr45-8vmc-qm54), [cookie attributes](https://github.com/nodejs/undici/security/advisories/GHSA-v3r7-h72x-cjcm). All affect≥7.0.0,<7.29.0. | Affected npm test dependency; no demonstrated app exploit. The primary blob advisory explicitly excludes native Blob and `fetch()` from that injection path. Upgrade jsdom-compatible Undici and rerun tests. This does not assess Node's bundled Undici by npm package version. |

The table covers every full-audit package record. Priorities are scoped to actual reachability: schedule development dependency upgrades and strengthen the development audit review, while addressing confirmed application P1/P2 defects first. No new production security finding is asserted solely from these scanner severities. Untrusted pull-request builds already execute repository code; these advisories should not be misrepresented as a novel privilege boundary bypass in that setting.

Validation records (cwd `/tmp/daedalus-review-457d338/frontend` unless stated; Node 22.17.0/npm 11.17.0):

- `PATH=/home/brandon/.nvm/versions/node/v22.17.0/bin:$PATH npm audit --json > /tmp/daedalus-review-evidence-457d338/backend/npm-audit-all.json 2> /tmp/daedalus-review-evidence-457d338/backend/npm-audit-all.stderr` — completed exit 1 with 9 reported packages; audit command succeeded in reporting vulnerabilities.
- `PATH=/home/brandon/.nvm/versions/node/v22.17.0/bin:$PATH npm ls @vitest/mocker vitest autoprefixer baseline-browser-mapping brace-expansion browserslist js-yaml postcss-selector-parser undici --all --json > /tmp/daedalus-review-evidence-457d338/backend/npm-audit-paths.json` — exit 0; exact dependency paths/versions verified against package-lock entries.
- `PATH=/home/brandon/.nvm/versions/node/v22.17.0/bin:$PATH npm audit --omit=dev --json > /tmp/daedalus-review-evidence-457d338/backend/npm-audit-production.json 2> /tmp/daedalus-review-evidence-457d338/backend/npm-audit-production.stderr` — pass,0 vulnerabilities.
- `/tmp/daedalus-review-evidence-457d338/backend/venv/bin/python /tmp/daedalus-review-evidence-457d338/backend/fetch_npm_advisories.py > /tmp/daedalus-review-evidence-457d338/backend/npm-advisory-details.log 2>&1` — exit 0;17 current GitHub advisory records via public `GET https://api.github.com/advisories/<GHSA>`; records persisted `npm-primary-advisories.json`. Web browsing independently opened every corresponding advisory.
- `/tmp/daedalus-review-evidence-457d338/backend/venv/bin/python /tmp/daedalus-review-evidence-457d338/backend/fetch_npm_primary_maintainers.py > /tmp/daedalus-review-evidence-457d338/backend/npm-maintainer-check.log 2>&1` — exit 0; all 17 maintainer resources HTTP 200,15 repository security advisories and 2 official patch commits; full responses persisted `npm-maintainer-primary.json`. Both scripts are read-only public metadata fetches with no credentials.
- Source inspection: `package.json`, `package-lock.json`, `vitest.config.ts`, `.eslintrc.json`, `postcss.config.js`, Tailwind content globs, frontend Dockerfile, installed jsdom dispatcher and application import searches (`npm-app-imports.log`). Standalone path comparison found none of the affected audit nodes. No exploit probes, dependency installs, fixes or lock updates performed.
- `git -c safe.directory=/tmp/daedalus-review-457d338 diff --exit-code -- package.json package-lock.json` — exit 0, manifests/locks preserved.



## Blocked checks and practical limits

No full run against real model/MCP/OAuth, external Hindsight extraction, real sandbox, or configured NV-Ingest/embedding/reranker/Milvus service pipeline was performed. Read-only source/native fixture tests do not substitute for those unavailable disposable integrations. Real production target calls, credential mutation, deployment, signing, image push and user notifications were excluded by the review rules. Kubernetes policy enforcement was not tested with a disposable enforcing CNI; chart rendering alone is not dataplane proof. arm64 dependency locks were inspected but images were executed only on amd64. No physical Apple-device/PWA installation or sustained performance benchmark was available.

Current report status remains partial end-to-end validation. All runnable local checks selected from Makefile/CI and independent reproduction checks have finished; none is hidden by a fail-fast aggregate. Passing local checks do not establish a defect-free or production-ready system.
