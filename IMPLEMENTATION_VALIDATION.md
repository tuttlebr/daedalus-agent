# Implementation validation

Validation is **partial**: six external-service, physical-device and model/load acceptance items remain blocked. A seventh item, ARM64 Redis runtime acceptance, remains unverified for deployments outside the selected amd64 Redis target. Applicable local CI checks pass, including the final HTTPS browser matrix (157 passed, 14 conditional skips) and all six application/Redis image builds and security scans. Earlier browser failures remain preserved. Passing these checks does not establish that the system is defect-free or production-ready.

## Baseline, isolation and evidence

Baseline: branch `main`, commit `457d338ad9eec9a285a57436b37213b5ba577477`. Initial `git status --short`, `git branch --show-current` and `git rev-parse HEAD` showed only four untracked documents: the original three review reports and user `review.md`. Their hashes are preserved and checked again after implementation. That implementation-validation phase ended before committing. Nothing was reset, published or deployed.

Applicable `skills/AGENTS.md`, Makefile, CI/release workflows, manifests, runtime entrypoints and test scripts were inspected. Application prompts/skills were review material. Final tests use a detached checkout with intended edits copied in; ignored local environment files and existing build outputs were excluded. Independent CI commands were run separately so early failures did not hide later checks. Pre-commit auto-fix hooks were not applied across the original review reports.

Actual paths used below:

```sh
R=/volume2/daedalus/datasets/daedalus-agent
S=/tmp/daedalus-implementation-457d338
E=/tmp/daedalus-implementation-evidence-457d338
V=/tmp/daedalus-review-evidence-457d338/backend/venv/bin/python
ST=/tmp/daedalus-review-evidence-457d338/root/static-venv/bin
PATH=/home/brandon/.nvm/versions/node/v22.17.0/bin:$PATH
```

Commands omit only ordinary `> "$E/<log>" 2>&1` redirection when the log is named. Displayed credentials are disposable test values. No real provider, user or production infrastructure was contacted for validation.

Durable evidence: [unpacked evidence directory](/volume2/daedalus/datasets/daedalus-agent-artifacts/implementation-evidence-457d338), [archive](/volume2/daedalus/datasets/daedalus-agent-artifacts/implementation-evidence-457d338.tar.gz), [SHA-256 sidecar](/volume2/daedalus/datasets/daedalus-agent-artifacts/implementation-evidence-457d338.tar.gz.sha256), [file manifest and exclusions](/volume2/daedalus/datasets/daedalus-agent-artifacts/implementation-evidence-457d338/MANIFEST.json), and [scan results](/volume2/daedalus/datasets/daedalus-agent-artifacts/implementation-evidence-457d338/scan/summary.json). The locally archived bundle preserves original review evidence, implementation command logs, probes, redacted renders, first browser failures, final HTTPS results, source patch/current files and the original review reports. All payload hashes and archive members verify. Large executable/dependency caches, private test keys, kubeconfigs, opaque archives and local `.env` are excluded. Browser traces are expanded for inspection and scanning. Default-config gitleaks and private-value checks pass. `E/<path>` maps to `evidence/<path>` in the bundle; original review logs are under `review-evidence/`. No archive was published remotely.

These complete component command ledgers are part of this report's evidence:

- [Backend exact commands, runtime probes and migration experiments](/volume2/daedalus/datasets/daedalus-agent-artifacts/implementation-evidence-457d338/evidence/backend/validation.md).
- [Frontend exact commands, baseline regressions and HTTP/Redis/browser fixtures](/volume2/daedalus/datasets/daedalus-agent-artifacts/implementation-evidence-457d338/evidence/frontend/validation.md).
- [Operations exact commands, image identities, scans, Compose/Kind/Cilium and retention experiments](/volume2/daedalus/datasets/daedalus-agent-artifacts/implementation-evidence-457d338/evidence/operations/validation.md).

## Runtime and boundary fidelity

| Environment      | Actual runtime                                                                                                              | Evidence scope                                                                                                                |
| ---------------- | --------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| Host frontend    | Node 22.17.0, npm 11.17.0, Next 15.5.25, TypeScript 5.5.4, Vitest/coverage 4.1.11, Playwright 1.61.1                        | Fresh locked install, source checks, build, unit/browser compatibility                                                        |
| Python tests     | Python 3.12.3, pytest 9.1.1, isolated venv                                                                                  | First-party behavior; builder conftest mocks major frameworks/services                                                        |
| Static Python    | Ruff 0.6.4, isort 5.13.2 with `--profile black -o redis`, Bandit 1.7.9                                                      | Configured source checks; default isort flags are not equivalent                                                              |
| Native backend   | NAT 1.8.0 commit `3c44584ef5de2531e2ff548408f0e4658b755a69`, HTTPX 0.28.1, PyMilvus 2.6.9                                   | Actual registration, 17 schemas, private adapters, approval receipts and OAuth refresh; external providers use local fixtures |
| Native frontend  | Node 22.23.1, Next 15.5.25, web-push 3.6.7                                                                                  | Packaged runtime and installed dependencies                                                                                   |
| Redis            | Stack 7.4.0-v8, immutable digest `798ab84d9f266936b034ab11c4d04a2b8e4b441884c5aa7d17ac951eefdf742a`; server 7.4.7/RedisJSON | Actual JSON/string storage, atomicity, TTLs, leases, pending entries and persistence                                          |
| Milvus           | Standalone 2.6.9 with embedded etcd/local storage/auth; additional MilvusLite 2.5.1                                         | Actual IDs, index semantics and interrupted migration; no HA/concurrent-writer claim                                          |
| SeaweedFS        | 4.46, central digest pin, synthetic S3 keys                                                                                 | Object expiry and allocated-byte reclamation                                                                                  |
| Network/upgrade  | Disposable Kind, Cilium 1.20.1                                                                                              | Actual policy enforcement and persisted Redis upgrade/rotation/rollback                                                       |
| Scanner database | Trivy DB updated 2026-09-11T19:05:14Z                                                                                       | Time-bound advisories; full npm audit separately covers development dependencies                                              |

## Final isolated checks

| Check; cwd                                                 | Exact command                                                                                                                                                                                               | Result                                                                                                             | Log under E                                                               |
| ---------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------- |
| Install; `S/frontend`                                      | `npm ci --legacy-peer-deps`                                                                                                                                                                                 | PASS                                                                                                               | `root/final-npm-ci.log`                                                   |
| Lint; `S/frontend`                                         | `npm run lint`                                                                                                                                                                                              | PASS                                                                                                               | `root/final-frontend-lint.log`                                            |
| TypeScript; `S/frontend`                                   | `npx tsc --noEmit --incremental false`                                                                                                                                                                      | PASS, empty output/exit 0                                                                                          | `root/final-frontend-types.log`                                           |
| Unit coverage; `S/frontend`                                | `SESSION_SECRET=implementation-disposable-session-secret npm run coverage`                                                                                                                                  | PASS, 860 tests; 174 opt-in/skipped cases; all configured gates pass. Aggregate lines 49.51%, branches 41.01%      | `root/final-frontend-coverage-third.log`                                  |
| Real Redis; `S/frontend`                                   | `REDIS_URL=redis://default:implementation-disposable-password@127.0.0.1:16480/1 SESSION_SECRET=implementation-disposable-session-secret BACKEND_HOST=127.0.0.1 BACKEND_PORT=18099 npm run test:integration` | PASS, 195 tests/10 files, 76.70 s                                                                                  | `root/final-frontend-redis.log`                                           |
| Python final coverage; `S/builder`                         | `$V -m pytest --cov --cov-report=xml:$E/root/final-python-coverage-third.xml --cov-report=term-missing --cov-fail-under=65`                                                                                 | PASS, 1,404 tests/4 integration skips, 76.29%, 44.48 s                                                             | `root/final-python-third.log`                                             |
| Python real Redis; `S`                                     | `PYTEST_USE_REAL_REDIS=1 REDIS_URL=redis://default:implementation-disposable-password@127.0.0.1:16480/2 $V -m pytest builder/tests -m integration -v`                                                       | PASS, four actual Redis tests; 1,392 deselected at that snapshot. Later migration edits do not change Redis code   | `root/final-python-redis.log`                                             |
| Python lint; `S`                                           | `$ST/ruff check builder`                                                                                                                                                                                    | PASS                                                                                                               | `root/final-ruff-second.log`                                              |
| Python format; `S`                                         | `$ST/ruff format --check builder`                                                                                                                                                                           | PASS, 163 files                                                                                                    | `root/final-ruff-format-second.log`                                       |
| Python imports; `S`                                        | `$ST/isort --profile black -o redis --check-only builder`                                                                                                                                                   | PASS                                                                                                               | `root/final-isort-second.log`                                             |
| Python security; `S`                                       | `$ST/bandit -r builder -x builder/tests -ll`                                                                                                                                                                | PASS, no medium/high issue                                                                                         | `root/final-bandit.log`                                                   |
| Generated protocol; `S`                                    | `python3 scripts/generate_protocol_types.py --check`                                                                                                                                                        | PASS                                                                                                               | `root/final-protocol-check.log`                                           |
| Next build; `S/frontend`                                   | `NEXT_PUBLIC_WEBSOCKET_URL=ws://127.0.0.1:15001 NEXT_PUBLIC_WS_FALLBACK_POLL_INTERVAL_MS=1000 NEXT_TELEMETRY_DISABLED=1 SESSION_SECRET=implementation-disposable-session-secret npm run build`              | PASS; routes and nine precache URLs generated                                                                      | `root/final-frontend-build.log`                                           |
| WebSocket; `S/frontend`                                    | `npm run build:websocket`                                                                                                                                                                                   | PASS                                                                                                               | `root/final-websocket-build.log`                                          |
| Worker; `S/frontend`                                       | `npm run build:stream-worker`                                                                                                                                                                               | PASS                                                                                                               | `root/final-worker-build.log`                                             |
| Browser matrix first run; `S/frontend`                     | `E2E_SKIP_BUILD=1 npm run e2e`                                                                                                                                                                              | FAIL, 152 passed/14 skipped/2 failures, 7.9 min                                                                    | `root/final-browser.log`, `root/browser-first-matrix/`                    |
| Browser matrix after SVG fixture correction; `S/frontend`  | `E2E_SKIP_BUILD=1 npm run e2e`                                                                                                                                                                              | FAIL, 153 passed/14 skipped/one recurring WebKit route-loading failure, 7.7 min; same production build             | `root/final-browser-second.log`                                           |
| HTTPS production build and initial smoke; `S/frontend`     | `npm run e2e -- --project=chromium --project=mobile-webkit --grep 'real login retains\|streams a chat completion\|logs in through'`                                                                         | BUILD PASS (Next, WebSocket, worker); smoke FAIL, 3 passed/one Chromium service-worker fixture-certificate failure | `frontend/https-harness-smoke.log`, `frontend/https-smoke-first-failure/` |
| HTTPS smoke with generated-certificate trust; `S/frontend` | `E2E_SKIP_BUILD=1 npm run e2e -- --project=chromium --project=mobile-webkit --grep 'real login retains\|streams a chat completion\|logs in through'`                                                        | PASS, 4/4, 9.2 s; actual session, WSS and service-worker checks                                                    | `frontend/https-harness-smoke-second.log`                                 |
| Final full HTTPS browser matrix; `S/frontend`              | `E2E_SKIP_BUILD=1 npm run e2e`                                                                                                                                                                              | PASS, 157 passed/14 conditional skips, 171 total, 6.8 min, exit 0                                                  | `root/final-browser-https.log`, `root/browser-final-https/`               |
| Final architecture gate regressions; `S`                   | `$V -m pytest builder/tests/test_image_vex.py builder/tests/test_deployment_image_inventory.py builder/tests/test_ci_makefile_parity.py -q`                                                                 | PASS, 19 tests; final extra scanner-architecture regression included in the later broad Python run                 | `root/final-image-gates.log`                                              |

The root Redis fixture binds only loopback port 16480 with separate logical databases. The inspected browser harness owns Compose Redis/Seaweed on 16379/18333, HTTPS frontend 15000 (proxy to internal HTTP Next 15002), HTTP backend fixture 18000 and WebSocket 15001. WSS upgrades use the same HTTPS origin. It removes its services/volumes afterward. Browser projects cover Chromium, mobile Chromium, mobile WebKit, tablet WebKit and 320px layout; conditional skips are not physical-device evidence.

New deployment scripts/tests have explicit static checks in the operations ledger. Final migration files were rechecked after broad static checks. Native tests run actual pinned packages independently of pytest's framework mocks. The constituent CI checks were run separately instead of a fail-fast `make ci` invocation.

## Focused evidence and limitations

| Contract                                     | Observed evidence                                                                                                                                                                                                                              | Practical limit                                                                                                 |
| -------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| RV-001/002 ownership/accounts                | Distinct Redis identities cannot reserve/mutate another user's record. Legacy JSON/string, one-winner reservation, expiry/reuse, 12 concurrent updates and orphan suppression pass. Removed users fail login/API/WS eligibility                | Delete after final authorized save can still race retention/publication; config changes require process restart |
| RV-003/017 approvals                         | Native exact approved MCP execution records actual error as failure; request provenance rejects forged/cross-request replay markers                                                                                                            | Local tool fixtures, no real remote grant operation                                                             |
| RV-005 SVG                                   | Actual Sharp rasterization and browser direct navigation verify raster MIME/CSP, visible image and absent script effect                                                                                                                        | Cached pre-upgrade responses retain old lifetime                                                                |
| RV-006/007 memory                            | Native tools, actual Redis and local Hindsight HTTP clear only owner caches; replacement retained/verified before captured old records are removed                                                                                             | Remote Hindsight internals/IAM not proved                                                                       |
| RV-008 retries                               | Actual HTTP append then 503 results in one submission                                                                                                                                                                                          | Ambiguous remote outcomes remain unconfirmed                                                                    |
| RV-009/011 concurrency                       | Real Redis preserves goals/config/run updates. Actual child heartbeat, SIGKILL, lease expiry and fresh-process reconciliation avoid provider replay                                                                                            | Interrupted image job is explicit unconfirmed failure; no real model used                                       |
| RV-012/014/015/016 failure handling          | Failed stop retains tracking; real silent HTTP body canceled/deadlined; ingestion SSE requires completion; actual error-only component exposes Retry                                                                                           | Generic chat clean EOF remains documented compatibility                                                         |
| RV-018/026 policy                            | Empty/malformed citation ledgers enforced; explicit empty operations deny all; native schemas checked                                                                                                                                          | Shared types do not enforce model obedience or unify all sanitizers                                             |
| RV-019/024 push                              | Actual encrypted web-push under enforcing Cilium: disabled denies, CIDR/FQDN permits synthetic provider 201, unrelated host denied                                                                                                             | No real provider/subscriber contacted                                                                           |
| RV-020 counters                              | Real Redis increment/expiry and legacy repair; subsecond retry stays positive                                                                                                                                                                  | Existing outage availability policy unchanged                                                                   |
| RV-021/022, IR-007/008/009 resource lifetime | Shared blob references, late-load release, actual process/descendant reaping, output caps, native browser compression/transaction commits                                                                                                      | Browser cache tests are not physical-device guarantees                                                          |
| IR-002 thread ownership                      | Actual blocked Python thread retains operation lock after coroutine cancellation until settlement                                                                                                                                              | External NV-Ingest late writes/cancel remain blocked                                                            |
| IR-003 retention                             | One-minute object unreadable at 60.1 s while 2,101,248 bytes remained; automatic idle-volume GC reclaimed data by 125.2 s, seven-day control remained                                                                                          | Shared later writes can postpone physical reclamation; no secure-erasure claim                                  |
| IR-028 migration                             | Actual Auto-ID upsert first produced nine target rows from five source rows after retry. Strong reads/insert-missing now preserve all five IDs/contents through committed-batch disconnect, resume, replay and rollback; manual-ID Lite passes | Quiesce writers; ambiguous nonempty legacy targets need review; neither collection deleted                      |
| IR-030 briefing                              | Actual registered runner recovers malformed envelope/truncated collection with complete canonical HTML SHA256 `abe3fd204e42196a7b2fe6b2e4ce35d8a7f7caf41e0ccf224506ecec7d5368e0`                                                               | Invalid schema/source/HTML still explicitly rejected; no live sandbox called                                    |

## Measured resource changes

These bounded synthetic experiments measure Python allocations, not process RSS, image pixel memory, GPU memory, sustained load, throughput or production latency.

| Experiment                              | Before                                             | After                                                                                   | Logs under E                                                             |
| --------------------------------------- | -------------------------------------------------- | --------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| Sixteen repeated 1 MiB image references | 16 fetch/decode allocations; peak 18,208,845 bytes | One fetch/shared bytes; peak 2,449,972. Repeated multipart output still consumes budget | `root/image-budget-before.log`, `root/image-budget-after.log`            |
| 8 MiB HTTP body/1 KiB test cap          | Peak 16,823,537 bytes                              | 368,673; streamed raw cap and compressed-response rejection                             | `backend/fetch-buffer-baseline.log`, `backend/native-fetch-buffer.log`   |
| 8 MiB child output/1 KiB test cap       | Peak 16,813,202 bytes                              | 554,607; process group killed/reaped on overflow                                        | `backend/script-output-baseline.log`, `backend/native-script-output.log` |

## Deployment/security checks

Exact commands, digests, image IDs, fixture setup and first failures are in the operations ledger. Compose rendering uses the template environment, never local `.env`. No live cluster was targeted.

- Helm lint/default/custom renders, central image inventory and native nginx syntax for Compose/default/custom TLS pass.
- Native Compose ACL tests cover default/named users, denied anonymous/admin commands, JSON and stream pending-state persistence. Ten disposable Kind revisions exercise persistence, TLS/ACL/certificate rotation, upgrade and rollback.
- Full and production npm audit pass with zero reported vulnerabilities. Filesystem HIGH/CRITICAL and source/history secret gates pass; package-lock inventory is explicit. A separate scan of volatile generated Next/Playwright output retained generated-key/example-string matches and concurrent-removal errors; it is not counted as a clean scan. The explicit tracked/nonignored source snapshot (851 files), history (537 commits) and all six production images scan clean. Three later new HTTPS test files and their updated callers are included in the final curated source/patch scan. Trivy warns about the existing root uv-lock parsing limitation; native installed Python image scans supply runtime inventory.
- Upstream gates scan both declared architectures explicitly and verify scanner architecture. Raw Seaweed reports retain the affected gRPC finding. Separate AMD64/ARM64 VEX records require exact advisory/component/version/digest/architecture and ELF/symbol evidence from an unstarted container. They expire on 2026-10-11 and fail closed on drift, missing evidence or new findings. Primary advisories and raw reports are preserved.
- All six final application/Redis images build and scan with zero HIGH/CRITICAL findings or secret matches, and their identities are recorded by operations. Both backend architectures pass native NAT contracts; both frontend architectures pass Node/Sharp PNG generation. ARM64 Redis reports its version but refuses server startup at the emulated `ARM64-COW-BUG` kernel safety probe. That guard was not disabled; ARM64 server/module/persistence acceptance remains unverified. The later full-system guest experiment did not reach Redis and was stopped on user steering; custom Kubernetes values now require amd64 Redis nodes. These six images contain the final production application changes. The subsequent HTTPS harness edits affect test infrastructure only (e2e assets are Docker-excluded); the final isolated Next/WebSocket/worker build uses the new WSS fixture configuration. Later edits only affect test infrastructure and Helm placement; no production application changes followed the accepted image builds.

## Failures retained and test quality

Before-fix evidence includes removed-account login, output buffering, cancellation, blob ownership, IndexedDB transaction inactivity and discarded briefings. Native Milvus disproved a mocked assumption that Auto-ID upsert preserved IDs and led to another verified correction. Counts overlap across focused and full suites and must not be summed.

First broad frontend coverage failed two stale replay assertions requiring destructive text removal. Those now require intended text preservation; transport-deduplication assertions remain. A second run passed tests but missed the rate-limiter branch gate. A meaningful zero-second TTL regression now requires a positive retry delay; the unchanged gate passes on the third run.

The first browser matrix retained two failures. The new SVG probe's separate API client omitted the Secure cookie over HTTP localhost; the corrected probe uploads through the logged-in browser. Existing mobile WebKit login returned HTTP 200 and requested the next route, but three route scripts never completed their bodies. The trace does not establish an auth failure or service-worker root cause. Across both full matrices the timeout occurred twice. A bounded service-worker allow/block comparison did not reproduce it; one WebKit target crashed and is retained as a failed trial. Separately, WebKit stored no session cookie and returned 401 from actual `/api/auth/me` over the production HTTP-loopback fixture, while Chromium retained its Secure cookie and returned 200. Cached login UI state is therefore not proof of WebKit session continuity. The synthetic HTTPS discriminator passed 3/3 real session checks. The corrected test harness uses one temporary certificate and verifies authenticated browser requests after login. Chromium service-worker fetching required a narrowly scoped allowlist of that generated certificate's SPKI; the first certificate failure is preserved. The final smoke passed 4/4 and the full HTTPS matrix passed 157 tests with 14 conditional skips, including both previously failing WebKit cases and three new service-worker-control cases. Real WebSocket assertions still require connected and chat_token frames. Retries/timeouts, production cookies and application service-worker code were not weakened. The exact cause of the earlier intermittent route-body stall remains unproved; one complete pass does not establish absence of all future stalls.

Other retained harness failures: mounting all of `/workspace` hid the native venv; default isort disagreed with configured Redis classification; test venv lacked Ruff; RedisJSON fixtures initially missed the real string-key error; native Milvus probes needed strong counts/valid query filters; Seaweed rejected a bucket collection override, then passed with separate buckets. Actual service guards were retained. No test/scan was disabled or weakened.

Real local HTTP, Redis, SDK, process and browser tests remain distinct from mocked providers. Native NAT registration/adapter checks do not prove real-provider correctness, source quality, grants or remote durability.

## Blocked acceptance

| IDs        | Prerequisites and remaining target                                                                                     | Disposition                                                                                       |
| ---------- | ---------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| IR-001/002 | Disposable full NV-Ingest extraction/embedding/object/vector stack, two identities and controllable long jobs/restarts | Component/thread checks pass; full pipeline and remote cancellation/late-write settlement blocked |
| IR-004     | Actual external VLM fetcher/model, isolated DNS/redirect fixtures and synthetic endpoints                              | Caller guards inspected; external SSRF behavior unproved, no speculative fix                      |
| IR-012     | Test-account OAuth grants/scopes, disposable Hindsight/object IAM and controlled restart                               | Native refresh/adapters pass; actual remote reuse/IAM blocked                                     |
| IR-014     | Compatible ARM64 runtime for any future ARM64 Redis deployment; current custom values select amd64                     | Build/scan/executable evidence does not satisfy server acceptance                                 |
| IR-015     | Physical iPhone/iPad installed PWA, keyboard, safe-area/offline/update workflows                                       | Linux WebKit is insufficient                                                                      |
| IR-029     | Isolated model capacity, representative sources, sustained-load fixture and observable resource/cost data              | Synthetic allocation measurements complete; model quality/load unmeasured                         |

[Integration acceptance](docs/integration-acceptance.md) and [external VLM disposition](/volume2/daedalus/datasets/daedalus-agent-artifacts/implementation-evidence-457d338/evidence/backend/external-vlm-disposition.md) record exact first-check commands, loopback/test credential requirements and observations for completing these checks. No full-completion claim is made while they remain unresolved.

Final source/static follow-up:`make tools-check` passed (`root/final-tools-check.log`). `root/final-static-combined.log` records exact argv/cwd for final Ruff, format and isort checks over builder plus the three new shared scripts (166 Python files), skill-link and generated-protocol checks; all exit 0. The initial isolated Git diff invocation lacked its per-command ownership exception and printed usage; `git -c safe.directory=/tmp/daedalus-implementation-457d338 diff --check` passes (`root/final-diff-check.log`). No global Git configuration was changed. Final lint and nonincremental TypeScript repeated after the SVG fixture correction also pass (`root/final-frontend-lint-last.log`, `root/final-frontend-types-last.log`).

## Requested Redis amd64 placement

The user requested node affinity selecting an x86/amd64 node instead of continuing the ARM64 experiment. `custom-values.yaml` now sets `redis.nodePlacement.allowedArchitectures: [amd64]`. Redis's shared placement helper combines this with the existing hostname allowlist in one required selector term, so both constraints apply. Other workloads retain their existing placement; generic chart defaults leave architecture unrestricted.

From the isolated checkout, Helm 3.17.2 lint and default/custom renders pass. Two additional renders verify architecture-only placement and an explicit empty architecture override. A retained parser checks actual YAML, allowed/forbidden label combinations and the five other deployments. Exact commands, working directory, outputs, scope and peer review are in [placement validation](/volume2/daedalus/datasets/daedalus-agent-artifacts/implementation-evidence-457d338/evidence/root/redis-amd64-placement/validation.md). No production node/PVC or live scheduling was checked. Existing node-bound storage must be accessible from an eligible amd64 node; affinity does not move data.

The stopped ARM64 experiment booted an ARM64 guest kernel under locally extracted QEMU/TCG, but its initramfs failed to mount the root filesystem before Redis ran. Logs and provenance are preserved in [stopped experiment](/volume2/daedalus/datasets/daedalus-agent-artifacts/implementation-evidence-457d338/evidence/operations/arm64-system-vm-stopped/status.md). Its process exit is not counted as acceptance. Owned processes/container were removed, Redis's guard was unchanged, and no host service/kernel was modified. IR-031 records the verified configuration request; IR-014 remains unverified for a future ARM64 Redis target. The prior application suites were not repeated for these Helm-only changes.

## Evidence preservation checks

The packaging script was inspected before execution. From `/volume2/daedalus/datasets/daedalus-agent`, `python3 /volume2/daedalus/datasets/daedalus-agent-artifacts/package_implementation_evidence_457d338.py --bundle` passed fresh curation, default-rule secret scanning, complete-manifest scanning, baseline source-patch applicability and original-review hash checks. `python3 /tmp/daedalus-implementation-evidence-457d338/packaging/verify_bundle.py` independently passed archive checksum, every payload hash/size, member inventory, unpacked equality, exclusions and original-report preservation. The final manifest is the authoritative inventory; the checksum is outside the archive to avoid a circular hash.

The default scanner runs from an empty temporary directory with inherited `GITLEAKS_*` options removed. Curated copies redact rendered test secrets, public scanner demonstration spans and fetched advisory telemetry fields; original evidence remains outside the bundle. Transformations and exclusions are recorded per file. Browser images/videos come from disposable fixtures; a text scanner is not a general visual secret detector. No local private credential match was found in the curated text. Report/source links and the 57 unique item dispositions were checked independently.

## Preservation and handling incident

Final source inventory records 161 intended changed/new files, all byte-identical between the shared working tree and isolated validation checkout (`root/final-snapshot.json`). Generated-file checks, complete diff review, secret scans and owned-service cleanup are retained with evidence. `docker rm -f daedalus-implementation-root-redis` exited 0 after the final browser run (`root/final-root-service-cleanup.log`); browser and component fixtures removed their own services, networks, volumes and temporary certificates. Pre-existing services were preserved. Original review reports and user `review.md` remain byte-identical. Temporary probes/build assets are outside the source diff.

One backend search accidentally printed a credential from untracked local `.env` into tool output. It was not used, intentionally copied into deliverables or sent to an external service. Subsequent searches exclude it; evidence packaging checks for private values without printing them. Real credential rotation was not authorized or performed.

## Local commit preparation and fresh testing

The subsequent user request authorizes a local commit to `main` and fresh isolated testing. The historical archive above remains an immutable record of the precommit implementation snapshot; later preparation changes and test results are recorded in [commit/testing evidence](../daedalus-agent-artifacts/commit-testing-457d338/). The first actual commit-hook run, including all 29 LOW Bandit findings and its failure exit, is retained in `precommit-first.log`.

Prettier changed only whitespace in Compose and the image inventory; parsed values remain equal. A formatter-only exclusion preserves the original three review reports, while their integrity and secret hooks still run. Security review of the new standalone validation scripts identified assertions that disappear under optimized Python; unconditional checks replace them. Intentional operator-controlled subprocess calls use argument vectors, resolved executables and narrowly documented scanner dispositions consistent with existing repository scripts. No scanner severity threshold or global exclusion was reduced. The separate evidence records focused regressions, final hook results and the resulting commit SHA. User `review.md` remains outside the commit.
