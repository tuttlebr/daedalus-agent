general:
  front_end:
    _type: fastapi
    step_adaptor:
      event_category:
        - LLM
        - TOOL
        - WORKFLOW
        - FUNCTION
      mode: default
  telemetry:
    tracing:
      phoenix:
        _type: phoenix
        endpoint: http://localhost:6006/v1/traces
        project: simple_calculator
