interface:
  display_name: 'Bubblewrap Agent Workflow'
  short_description: 'Run isolated commands and deliver verified artifacts'
  default_prompt: 'Use $bubblewrap-agent-workflow to run a bounded task and deliver its verified artifact through the sandbox tool.'
